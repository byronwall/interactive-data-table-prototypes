Alright, let's keep going. We've got uh table of data and we're trying to render it. That first thread talked a bit about filtering and the different ways of visually conveying filters and visually interacting with filters. Uh but let's assume for a second that just in raw. I've got a table, I want to see the table, uh give me the best way to control and see a table. Um the features that are important are uh especially when you have a large number of columns, uh three, five, ten, twenty, uh being able to control which columns are on screen is important. Sometimes you know exactly which columns are interesting, and maybe if you get 20 columns, only three of them are ever interesting, or four or five or ten are ever interesting. You know that ten of them are in there and they're just noise. Uh maybe only one in a thousand times would you ever actually look at any of the values in those ten columns. And so if that's what's going on, then you really need the ability to just by default hide those columns, never show them, don't let them interfere with the columns that you do want to see. And this is often important because usually those 10 columns will be mixed in with the other 20. And so if you're just doing a pure render the columns as they appear in the source data, it's uh it's a nuisance because the 10 that you don't want are extended, so you have to remove them. So that's uh one of the most important features is the ability to given 20 columns, just show 10 of them, or more importantly, given 20 columns, make sure that these 10 never appear, unless the user goes out of their way to go turn them on. And very often you'll need to have an initial step where you can say, I definitely don't want these. And so this gets into the idea of being able to store some settings that are specific to the user, specific to the list of columns that appeared. Really starts to depend on how the tool is going to be used repeatedly, or if it's just one time. However, it's done, you need a nice clean list of visible columns that persists in some way. Okay, so what does the column picker column looker thing look like? Well, it needs to have the column name, so that's a minimum. Or some way to control what's in or out. Sometimes that works perfectly. Other times it's a nuisance if the columns all have some common substring. It can be difficult to just pick the ones you want, or if the column names are really long, it can be a nuisance to try and find the ones you want. I guess that hints at another feature that's often very important is the ability to alias or label things. So if you've got column labels that are crazy long or have some repeating prefix or suffix, uh it can often be really helpful to give yourself a quick map where you remove the prefix, the suffix, or just provide an alias for the column name. Yeah. Okay, so we've got the columns, we pick the ones we want, or we've tried to. Other times you don't actually know the columns you want are. You need to see what's in there. And so if you're in a situation where you need to be able to see a peak of the data before you choose to show or hide the column, then it helps to have a nice compact representation of the data. If the data is numerical, then a sparkling can be a good way to do this. If the data is categorical, then listing the instances can be helpful. If the data is random unique strings over really long things or not samples of 6,000 characters or something, that gets harder. But somehow or another you've got to render a preview, a sample, a subset. And if the data is going to be big and nasty, then often you have to do it as sort of a tiered progressive disclosure where you first show the preview of the data for maybe one row, truncated. You can click and see an expanded preview of the data. At that point, that should give you enough signal that you can say, oh yes, this column is interesting or not interesting. Sometimes the best surface for seeing samples of the data is just the actual table renderer itself. Then being able to just use the column header controls to show and hide is probably the best way to do it. You can very quickly work your way through 20 columns, right-click on the header, uh show hide show hide kind of thing. Hiding is obviously easier than showing because they're visible, and then you choose the item. Once you've chosen to hide stuff, you then need to give the user an interface to show what's been hidden, so they can bring it back. This is kind of the same problem repeated, because now in order to bring it back, you might want to know what's in there. But honestly, sometimes the best solution is just to put it back in there, render it on the table, and then they can choose to hide it again if they want it. When it comes to these lists of subsets, sometimes the best you can do is to let the user store a list of field names that they like that they'd like to see together, ideally in the correct order, maybe even with some additional settings in there around column sizes or something. But saved views can be a really helpful way if you're going to interact with the same 20 columns but from a different lens each time. Okay, so all that is just in the spirit of let me show in the height columns. Once you've got columns visible, uh the other important features with respect to displaying columns is going to be the width of the column. This is one of those things that can really make or break a tool. The width of the column is important for a couple of reasons. It's important because sometimes the data just has an inherent width. It just feels really nice. It's kind of compactness to it. It can really support getting three or four things on the screen at the same time. If the data does not have an inherent width, maybe it's free text, maybe you got a lot of nulls and then the occasional value, then it's just really important that the user is able to resize the column. Typically, you want to be able to drag the column header like in the middle, the border between two columns. Drag to resize. In one implementation, you can just be changing the width of one column only, which means the rest of the table is getting bigger or smaller. So you're just changing one column in other implementations. You've decided that the total width of the table is fixed, and so then changing the size of one column necessarily changes the size of others. This can be real nuisance when you're trying to make one column bigger, now you're making another column smaller, and if you can't reorder the columns, it's like you've got this cascade of I need to change them all in order to get the final bit of width to distribute on the last column kind of thing, it's completely annoying. So you do better often to separate the column width from the table width, but that then puts you in the position of the table may very likely overflow its container, and now you've got horizontal scrolling on the table. Anytime you allow a table to show a horizontal scroller, it is an inherently terrible user experience. It's so bad for a couple of reasons. One is that very often the height of the table makes it so that you cannot see the scrollable nature of the table until you actually scroll down. And so it's really really easy to be unaware that the table has horizontal overflow. Until you scroll down. This is a little bit better on like a trackpad, touchpad, where you can just two-finger adjust your left or right. But even then it can be really hard to just remember where in the table you are because often tables don't have like a mini math that says, hey, you're looking at a middle five columns of 15. So what does that mean? It means that very often the ultimate constraint is what is the width of the container holding the table. And even though it sucks to be forced to distribute the visible width inside the container width, excuse me, to distribute the column widths within the container width, there's really not a better way of doing it. You if you're gonna let the columns end up totaling to something bigger than the container, you're not gonna be able to see some of them some of the time. So it's like you may as well just choose which ones are not visible rather than having it be some uh kind of after the fact decision because they just don't fit. Um but there's not a great way to convey to the user, hey, you made that column too big, so we removed six others. That gets uh definitely complicated if you're gonna support automatic resizing of columns. Alright, so let's talk about that. Uh, one of the most useful features in Excel is the ability to select a column and then double click its column header border, and it will automatically resize to accommodate the width of the data. Where this is really helpful is if you've got six, ten, twenty columns, and they're all sized for some default arrangement, but you know they only contain three or four characters. You can very quickly resize them all to just accommodate the three or four characters that they have. And then that'll shrink the whole thing and make it really convenient to display all of them. Where that works against you is if you've got a couple columns with a couple really long cells. Very often the auto resizing will get uh thwarted by that. You'll end up with some giant column width. I think in Excel it was like a column width of 250 or something. Once you get one of those suckers, though, it's just terrible. Um, even just making it smaller again, that's terrible. And so uh auto resize works great until it doesn't. Probably got the ability on an auto resize to pick some you know P90 value or something. Um just another idea that's like is incredibly rare that a column that is the width of the viewport is gonna be any good. Um it's just like uh unless the viewport's tiny, in which case you know what are you doing? Um but the uh double click to auto resize, it's very helpful. Um it's especially helpful when it leads to more columns being visible on screen than before. Alright, what else do we have with columns? Okay, uh, so we've chosen which ones we want to see. We've got the ability to show and hide. We've got the ability to resize but drag in the header. Being able to reorder the columns is important. So typically this is done by being able to click and drag the header into a different column left or right. Sometimes this is done by having an alternate control for moving things around. So you could have a list of labels above that list of all the labels. You drag them there. That then changes the column ordering. But you're doing your drag and drop in a like a secondary place not on the header itself. Yes it's fine. It can be useful maybe to see the full field list. If you can't see all of the column headers again getting back to that horizontal overflow thing. But I think honestly just rendering things in place one time is probably better than having a second thing that's listing all the columns. And so if you need to reorder them probably the best way to do it is to just drag the header left or right. Keyboard shortcut might be nice for this I don't know if Excel had one I guess you could always cut move over a couple select the whole column and paste. So that sequence if you're in Word it always had the alt shift shortcuts, alt shift left right to move things around. So that'd probably feel pretty good to just quickly move a column left or right shift left to right left right. But being able to move a column around is very helpful. Maybe it's going from general but specific, maybe it's going from categorical information over to numerical values. Imagine purchases where you've got a date is nearly always on the left, description, category, price, something like that. Other times the ordering you may not know in advance what the important order is or the data might be messy in a way that's not obvious when you start and so being able to quickly change the order or to play with the order can be helpful. Alright so we've changed the order of the columns what are we going to do after that? Column formatting display is uh probably the next most useful thing. So typically an entire column has a common data type common data format that makes sense for it. So if it's currency values you'll want to display maybe dollar sign if it was not in the source data. Might want to show so many digits after the decimal place might want to show a date with a specific format. Date formats are one of those areas where the tool can really make or break how useful it is being able to just show the month or the day or timestamp or something that makes sense for the data that you have on hand can be really helpful. Then let's you quickly work with the same data from different lenses very easily. Sometimes the data is messy. So most common example is you've got missing cells or missely typed cells. In the case of missing cells, you might have a column that's entirely null, so it's all empty. You might want to convey that somehow, but you also probably don't want to waste a whole lot of space saying, hey, this thing's all in holes. So being able to detect that is half the battle, being able to show something useful is the other half of the battle. Other times you would like to display something in a given format, but the actual data in the cell doesn't support it. So you want to show timestamps, but then somebody comes through with the string, and now you're trying to parse the string of the date, it fails. What do you show in that case? You would do something different. A lot of ways to handle that. In other cases, something that a cell lets you do, you can change the formatting at the cell level, so not just at the column level, but the cell level. So this is like a simple way to use a color scale. At this point, you're starting to dip into the world of just data visualization. Conditional formatting often creates a scale of some sort, color, format, etc. And then you're choosing to render the data differently based off of its value. That sort of thing can be really helpful. Building out the rules can be really difficult, especially in Excel. You could create some really gnarly rules that had really difficult to understand ways of being applied. For a general purpose tool, you really really, really don't want to get into that stuff. You'd honestly do better. You'd honestly do better to just define a derived field with a computation and then go apply some simple color scale to that. But for various reasons, you might be constrained on the number of columns you could display. And so you definitely could do some madness in conditional formatting instead of doing the correct thing of just deriving a new column and then rendering based off of that value. Yeah, and I guess that's uh it's a common theme in Excel. Very often you were forced to render things. You're forced to couple the calculation to the rendering in a way that made it really hard to uh disentangle them and complicated spreadsheet. Um maybe we've got to do really wacky stuff just because it wants to be red all the time. Okay, so we've chosen what columns are visible, we've set their size, we've set their order, we've set their formatting. Uh is there anything else useful you might do with a column that is independent of the rows? Uh no, I don't think so. That feels pretty complete, or something.