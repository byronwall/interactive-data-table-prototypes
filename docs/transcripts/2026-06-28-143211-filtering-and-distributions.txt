Alright, here we go. See if we can finish this. This is the continuation of building an interactive data table. We've discussed everything about columns, formatting, choosing. We've discussed most things about rows with respect to rendering, height, all those concerns. Now it's time to talk through maybe the last two pieces. One is filtering the rows, how and when to do that, what controls to give the user, where to put the controls, and how to convey what's filtered. And then in addition to filtering rows, talk through all of the additional niceties related to showing distributions. And often showing distribution is possible to filtering. And so there's kind of go hand in hand. Is to make it simple and easy for the user to filter in the way that you would expect them to relate it to make it clear, abundantly clear when they are looking at a filtered subset as opposed to the entirety of whatever the original data was supposed to be. The making it clear that they're filtered can get really really really gnarly if the application involves some amount of rendering new tables, which are a subset of old tables, because now you get this sense of the whole data, the data that they're looking at, and then some subset or slice of either of those, and how to convey that. But on the point of filtering, the first thing is to decide what sort of filtering is supported, and probably the top-level choice there is whether you're going to allow for global filtering, so filtering some value across all columns versus column-based filter, where you're filtering all of the rows based off of a column matching subcondition. Simple way to combine is either an and or and or, but if you're going to allow combining, you then have to make it abundantly clear that multiple values are being used for filtering and the way in which the filter is being applied. So let's do global filtering because it's easy, less common. And it tends to show up more in less of a data intensive application and more of just a general information kind of thing. It's pretty rare in a data intensive application for somebody to ask, oh, does this value appear anywhere in the data? It tends to be the case that people know what they're looking for, or at least in what column it might appear most of the time. It's pretty rare in a serious data application to then say, oh, does this appear anywhere? Although I guess, I don't know, maybe it's not that uncommon. Excel has control F. You just don't really think of it as a filter. It's more of a find this thing is displayed somewhere. But anyway, it's global filter. Nearly always it's text box somewhere in the top, top left. Type into it, it starts filtering the rows. The biggest thing you're offering a text box to filter is whether you're filtering based off of an exact value or off of a fuzzy match. Exact value is pretty straightforward. The characters must appear in the order that they're rendered. For a fuzzy match, you get some more choices. You can choose in what ways is it fuzzy. The one that tends to work the best is to support character omissions, but to not allow things to appear as a typo or out of order. A fuzzy search that is typo tolerant tends to just be way too permissive. Once you allow for random characters in any order, it can be really hard to understand what you're going to get when you type in characters. But a fuzzy search that is typo intolerant, which is to say it supports characters being missing but not out of order. That tends to be a very powerful search because most people can think through the values that they would need to type in order to filter the thing to something that they think might be present. This is especially relevant if you're dealing with things that are machine generated either identifiers, code, labels, anything like that where you are needing to supply characters that don't strictly match normal words. For example in the engineering context you're often needing to search amongst identifiers various sorts of labels various sorts of machine generated strings that are already kind of short but often that are repeating with a common prefix or suffix. And so you're typing just enough to get a piece of the suffix or maybe a piece of the prefix and then you type the rest of it a fuzzy search can be really good at that. Okay so we've got a global filter we've decided if it's going to match exactly or fuzzy. And you can do both you can have the thing match exactly and fuzzy, and then you apply a ordering on the rows so that the exact matches appear before the fuzzy matches. That's pretty common. But if you're going to search globally, you then have to decide are you searching across all columns or just the columns that are visible? It tends to be difficult to convey to the user, hey, this is what we found. If the column that it was found in is not visible, and this kind of hints at the next thing, which is if you're going to offer filtering, especially filtering across multiple columns, you really really really need to convey here is the piece that's matching. Simply this is done with a mark, yellow background, yellow border kind of thing. If you do that, you want to make sure that the mark doesn't alter the display of the word. So if you type in POS and it matches on position, let's say, uh, you still need to see the word position laid out normally. A lot of systems will get this wrong and they'll put padding on the mark, and so that will look like POF space because you've padded the mark. It's really unhelpful. You have to make it look like normal text that just happens to have uh an indicator that it was search target. Um if you're doing fuzzy matching, this is much much harder because often the fuzzy match can find characters that are far apart, and so it can be much harder to convey matching on those. Um the world of a fuzzy match, maybe that's just good enough. Uh especially if you're gonna do a fuzzy match across all columns, you sort of just have some mercy of that eating what it gets. It can be very hard. Or it's not hard, it's just visually distracting if you're gonna show uh a fuzzy match across multiple columns, especially if you're only two or three characters in that's matching a ton of stuff. Um it's just very noisy trying to convey, hey, here's what's matching. You almost do better on a fuzzy match to just say this is getting the user in the ballpark, and you're not really trying to convey, oh, here's the exact match. Um I guess that's kind of the point, is in a fuzzy match you you don't expect to match exact words, and so it's way less useful a visual feedback to then convey, oh, here's the parts that are matching. Um really that fuzzy match can't help convey to the user, hey, here is the style of fuzzy matching that we're using. But it's not particularly useful to convey here's how your query is being used against the data that is available. Uh if you're gonna offer fuzzy and exact matching, uh it always works better to let the exact match float to the top, especially if you've got an exact match that is exact, uh, which is to say in some cases you're matching uh relatively short strings. Uh at Allison, this was always uh P underscore on for oncoming pressure. And so if the user types in P underscore on, you have to surface that at the top because there's literally no other characters they could supply to filter it further. And so if you do anything other than that, you give the user a real chore if what they wanted was the exact match on P underscore on, because now they have to go search for it separately or scroll or something, because again, there's literally no other characters they could supply to filter that thing more. Uh I guess that's the general truth, which is that if you're doing string-based matching, uh short strings uh just give you limited surface area to match them. Yeah, if you only got four characters total, and the user types in three characters, plus you're gonna type the fourth character exactly. Uh there's not much more for them to type. And but if they need to type one more character to get that thing to show up at the top, maybe giving them a bit of a chore. Because they have to know the exact character to type next, but it's a thing they're looking at. It does not include the one they're looking for because uh it's only match two characters so far. Uh it can be a poor experience. All that to say when you're offering filtering, you have to give some serious thought to the rows that are displayed. What's the ordering metric? Because very often the thing that seems like not a great match is actually exactly what they wanted, just because it's got uh short strings. Uh another consideration on global matching, especially if the data is JSON backed or some sort of key value back. Uh it's very tempting to just do a quick uh index of kind of thing. See if the string appears in the string. But if you do that on the stringified JSON, you'll end up with all the keys being matched against also. Or if you've got a fuzzy match, you'll have some weird mixture of keys and values contributing to the selection. And so you can't do that. Uh just doesn't work. It doesn't work if you're trying to provide a good experience. Instead, what you have to do is iterate through all of the possible values and match on those. This gets uh tricky if you've got uh some sort of hierarchical structure because it might be that the value you want to match on is a sub-key down in the tree. And I don't know what the answer there is. Um this gets additionally complicated if uh you support arbitrary metadata where now the keys actually are something the user might want to search on because they've supplied them. Um, if you're gonna provide a global search across that kind of data structure, you just have to actually set up the search so that it is uh intuitive, which is to say the thing that you're searching over is the stuff that the user expects to be searched over, and you can't be accidentally searching over some internal development detail, especially if the user can't see it. Okay, so that gives us a global text entry filtering. Start there because it kind of highlights the issues that we'll repeat when we support just uh text entry filtering. Often uh a single text field for filtering can get you really really really far, and especially considering the surface area it takes up, which is quite minimal, and the ability to do substring matching and the results, it can be a very very effective way to filter a lot of data sets. This is especially true if the client or the table rendering has access to all of the data, because then it can apply whatever algorithm it wants to go search, and then it can surface orders, surface rows any order that makes sense. And uh if all the data is on hand, it's uh it's a very very effective way to quickly go filter things. One other comment there is if you just have a single text field, you've also got the ability to supply some sort of uh almost like a pseudo query language where you can have the user supply you know uh gate colon and then the thing to match for. Um a good example of this is then the GitHub PR and issue searching. They give you a single text field to go search across things. You can type in text, but then you can also type in uh key value pairs or other sorts of uh identifiers, custom language, etc. That they then respond some query that is manual with relatively minimal input from the user. And decouple that sort of thing with uh helper inputs, uh little maybe tool tips or auto complete things, so that's a single input. It's actually much more of a kind of advanced query editor thing. You can get really really really far with that and a single input. Um it kind of hints at where we're headed, which is behind the scenes, you're building a single query, and yeah, it might have column-based filtering, but at the end of the day, it still is just a single query that can be expressed as a single query, and maybe it's got some ands and some ors, or it's got some column criteria attached to it. Behind the scenes, you're just building a single query. And so how you choose to render that query is a little bit of a choice that you can make. You can choose to render the whole thing in a single text input, and that may work surprisingly well. You can choose to render bits of it in a single text input. And then if the user is supplying column filters, you can choose to show those in that same text input. They can be edited there. But you can also choose to render the column filter information on the columns result. But behind the scenes, there's still just one query being done, and you've got some data structure describing that query, and you're choosing how to let the user see the query, edit the query. And once you know you're going to do something like supporting single column queries, single column filters, you've then got the ability to go build a much better interface for supplying both the values and the keys, possibly, for that single column filter. Reason why it might be keys and values is it's entirely possible that a column stores a whole data structure inside of it. And so now maybe you're doing slightly more advanced key value pairs on a column. Because again, we're just building up this robust detailed query. And it might have some nested filtering inside of it. Okay, so we sort of marched our way from a global search off of a single text input to a global search with the text input that's doing slightly more advanced stuff. And we have this idea that we're building out a single query object. And so then from there. a way more ergonomic filtering experience specifically if you know that the data type is numeric the data type is categorical you know that it's a Boolean you know that it's free text all of those are bits of information that can be used to build a much better filtering experience the worst you can do although I guess it's a starting point is to put a single text field at the header of each column and just go match for substrings or existence. This tends to suck though because if you're wanting to search for numeric data let's say you might remember that the purchase price is somewhere between 80 and 90 dollars but how are you supposed to do that if you just have a text input you can type an eight now you get everything that has an eight in it you can type 80 but if it wasn't 80 dollars exactly you can type 80, 81, 82, 83, 84. Well that sucks. The obvious thing to do there is to supply some sort of data type aware filter and to let the user build it with the fewest number of clicks needed. And so if we're just again a single text input what you'd like to type in there is greater than 80 because now that at least gets you in the world of oh I know I was greater than 80. I knew it was between 80 and 90. Maybe you type in greater than 80 less than 90. But this uh free text input not the best because now you're sort of putting on the user to know in advance that they can type all these advanced queries and uh we started putting it on them to get all this stuff to be correct and to work. When you're in the world of searching, one of the worst experiences is when you confuse a mistake in the query language for the non-existence of the result. So maybe I attempt to type in greater than 80, but my engine doesn't actually support that because it's inferior, it's just doing exact string matching. Well, now all of a sudden the user thinks, oh, there's nothing in here that's greater than 80, because when I typed in greater than 80, it didn't work. It's a pretty terrible experience. And most people have been burned by enough of these inferior data table things that they've conditioned themselves to not try advanced inputs as filters because they so rarely work that you're just getting false negatives. Okay. So what are you supposed to do instead? Well, what works better is to just render a set of controls that makes it very clear what types of queries are supported. And if we want to have those fall back to or have some advanced text representation, you can do that. But you probably don't want to start there. Instead, where you start, if you know you've got a numeric input is with uh sorts of filters that you'd expect. So greater than, less than, equal to. Uh you just type in 80 and 90, you know, 80 tap 90, and with a display that makes it clear that you're doing a range filter. Uh boom, you've got your range filter. Now, you may have to give some consideration to uh when I type in 80, does that include the value? Yeah, probably. My suspicions when a user types in greater than 80, they would be surprised if 80 is missing, but 81 shows up. Really what they're trying to convey is I don't want 79.99. So I would tend to do it as a inclusive filter on both ends. Unless you know in advance that for some reason inclusive filtering non-gumeric data is unexpected. Oh maybe on the end value you would uh less than 90. The user expect to see 90 as a result. Probably not so maybe it's inclusive on the start and exclusive on the end. That'd probably work. If you've got uh integer data or even floating point data it helps to supply the exact match. But man exact matching on numbers basically never works. There's always something that goes wrong between the floating point representation and the integer or decimal representation. And so you kind of got to stay away from that if you're gonna offer it it's got to be slightly fuzzy or with some sort of tolerance epsilon kind of thing in there if you allow the user to type in 80.1 but technically the stored value is 80.0 you're gonna violate expectations in near mind that here's 80 point one maybe that means you have to round it in advance. Maybe that means you have to apply the I guess the first thing to that. Maybe you have to apply the numeric formatting and then good you string operations on that. Something to consider. Because if the display value is 80.1, even though that might internally be 80.08 or something, the user probably expects it to match if they're supplying an exact match. So something to consider. But in general, most people are not doing exact matches on numerical values if the numerical value really is floating point. Now they will do exact matches if their data is more categorical. Hey, what was the severity on this one, two, three, four? They're absolutely gonna just type in a one and expect it to work. But there are integers, it's a small set, it's basically a category. And this is one where if you know in advance what the possible values are, and if you know in advance that it's a small bounded set, three, four, five, six values, um, you do much, much, much, much better to just show all of them and to give the user a set of toggle buttons, because then the toggle buttons can represent uh include exclude. It's really easy to build a filter that is a multi value, uh, b or d. It feels real intuitive, it looks nice. Everything about that just works really well. The difficulty in that is determining in advance what the cardinality of the column is. But again, if you're on the client and you have all the data, uh, you should just always be computing that. Hey, what's the cardinality? If it's less than five and the strings are short or unique, like non-repeating, different topics, different prefix kind of thing. Uh just show them all, show a uh truncated version of it used for filtering on Hoover, you can show the full value. Um, but anything you can do to supply just a visual of here are the values, uh click to filter, and then a visual of hey, here's the filters being applied, click to unfilter. Uh, that is a user experience that's very hard to beat. The difficulty of doing that is just the space required to render all the toggles, and then the decision of do you render all of them all the time? Like up in a header, do you render them only on a trigger progressive disclosure? Do you render them uh on hover or some other scheme? Do you render them over to the left, like on the side somewhere, like a filter panel? Um you have some options, but in general, if you've got uh small set, low cardinality, uh you should just show them all and make it a toggleable uh selection. If it's more than that, let's imagine you've got uh well on the extreme end, uh every value is unique. You do really well to convey that every value is unique because then the user uh knows what they're up against, which is there's not an easy way to go get a group, they're all unique. That shows up a lot on uh identifiers, GUID, that kind of thing. Um it also can show up with uh oh, I don't know what timestamps are a great example. Uh they're often unique until you aggregate them. Um, so if you uh counted them all, you know that they're unique. Uh you say that, you offer just a text input, and then you make it clear whether it's exact or fuzzy. Uh exact tends to do better. Fuzzy might work, but it depends on the data. Probably the answer there is just if a user might know an exact value, they'll be quite irritated if fuzzy results are showing up because a lot of users are using filtering as a way of running little subqueries. And if they type something in and see three, they really want to believe that three is the actual count, not that two of them have an exact match and one was fuzzy matching. Um, so it's strings, low cardinality, high cardinality, and you get to the middle ground, which is like uh 10, 15 unique values with uh high concentration, like a non-uniform distribution. If you're in that world, you do well to convey that it is what's going on. So you want to get a toggle filters for the most common values. You want to make it abundantly clear that there's 20 additional unique values. Maybe you allow uh a hover to see them, like a progressive disclosure. You don't want to render uh 20 buttons, that's uh too many, but it can be very helpful to point in that direction and then to allow the user to opt into seeing them so they can filter. So that's numbers, that's strings. Uh sort of what we're marching toward there with these uh toggle buttons is the idea that you're conveying something about the distribution of the data visually, and then allowing the user to just interact with that distribution to do the filtering. If we take that back and apply it to numerical data, the obvious thing is to render a histogram of the data and to allow the user to click or brush for a between kind of filter. Works well, although often you've got a limited uh real estate for these distribution things. And depending on the magnitude or range of the data, it might be really difficult to go construct a query that's useful from a click brush. You might have prices that range over six orders of magnitude, but the user wants to filter on 80 to 90. Um, that's going to be a miserable experience with a brushing filter. But you might have prices or uh ratings that range from 0 to 100. You give that sucker, I don't know, 200 pixels of width. Now you're in business. You can filter that very precisely. If you're going to offer a distribution brush filter, you have to back it up with text inputs to override. And you also have to make it easy for the user to create a one-sided filter where they uh click and drag all the way to the side, make it clear that it's uh one-sided greater than 80, as opposed to 80 to 1 million, because then if you get one additional large value at the end, you've saved that thing as 80 to a million. You might all of a sudden be lying to them when they rerun the filter on new data. If you're rendering small distributions, uh charts histograms are pretty straightforward. Um next, I'll keep talking about filtering on dates because that's where aggregation becomes relevant. Uh and it also is where you can start to introduce more advanced visuals to convey both the distribution and to give the user something to click to do the filtering.