Alright, here we go. Let's figure out what are the ideal best features to include in an interactive data table explorer. So what are we starting with? You got table of data. It's got some number of rows in it, some number of columns. The ideal display really depends on how many rows and columns you have. And once you start looking at rows and columns, it starts to depend on what is the data, data types, data distributions inside those rows and columns. So I'm thinking the ideal set of features and controls depends a ton how many rows and how many columns you have. On the rows front, uh there's really a handful of breakpoints. If we've only got one or two rows, then it's not really an interactive table at that point. It's just like a list of values, and depending on how many columns you have, either the list of values is gonna look better horizontally or vertically, which is to say if you've got two rows and a bunch of columns, you should probably transpose the thing, make your columns turn into rows, render it that way. It's probably a whole not a whole lot of interactivity to be had when you've got such a small number of rows. It maybe matters what order you display them in. It's really much more about what are you trying to communicate with this table rather than some sort of interactive data exploration thing. Okay, so low row count, uh just not that interesting. Um there is probably something to be said about what is the best way to render uh two columns so that it looks good. Um you have to decide uh and really it's gonna be three columns when you render it because you got the column labels too, but there's definitely an art to making that look good when you've got so few columns. It really starts to matter how you group the rows, what sorts of uh topography weighting you use. Uh but it's not really an interactive data exploration thing, it's just like table as visual output. Um, so then if you get to more rows, at what point does it become interactive? Um, really it's when you've got more rows than can easily fit on the screen. That's one way to think about it. So if you've got uh super large screen, you can fit more rows in if the rows contain small, not tall bits of data, you can fit more of them in. Um but somewhere around uh 10 rows, you just render them all. Not so bad. Uh 30 rows. Somewhere around 30 rows is where you can't just render them all. You have to start making choices around what is happening on the screen at any given moment. And once you've done that, now it starts to matter what's the order that things are displayed in, and if you're filtering the rows at all, what are you choosing to show per side? And then how do you convey what filters are available? So I was thinking of that. The rows that you choose to display, it really depends on what the user is doing with the table. Sometimes they are just exploring the data. And so if they're just exploring the data, being able to sort and filter is really helpful because it allows them to quickly walk through subsets of the data that might be interesting. One of the things that happens when you're walking through subsets of the data that might be interesting is you're often trying to store little bookmarks or breadcrumbs of, oh, this was interesting. Oh, these certain rows were interesting. And sometimes it's the specific rows that are interesting. And so you might want to be able to tag or bookmark specific rows. Other times it's the subset of rows, so it's the actual filters that are interesting. This group of rows compared to the broader population is interesting. Sometimes it's like a second order concern where you do an initial filter, and now you're looking at that subset of data, and it splits it in interesting way. And so you want scatter chart, bar chart, that kind of thing. And then that kind of loops back around and is a little bit of a positive cycle because once you've got the ability to use graphics as your filtering mechanism, it then becomes just a much better way of filtering the data set. And then once you're using graphics to filter the data set, it tends to be less and less important which rows are actually visible on screen unless you're trying to go pick out some small subset after you've done some filtering or some other stuff.