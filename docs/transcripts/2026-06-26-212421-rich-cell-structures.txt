Alright, so let's sort out rendering a more complicated data structure into a single cell. This is the sort of thing that is uh very very common when you've got known data and you're trying to render it in a subformat, so dashboard or uh business application where you can spend all sorts of time getting the visuals to be perfect for the data on hand. The typical arrangement is either that you want to render multiple values into a single cell with a single line height, or possibly a wrapping line height. Typical situation there is that you're wanting to render maybe a list of bags, a list of something for some reason you've got some list and it makes sense for it to live in a single column. Um I don't know, maybe you've got a picture of the last three purchases and some people have one purchase, three, zero, and it looks better to just render uh some of those if they have them rather than giving yourself three dedicated columns where most of them are empty most of the time. It also lets you, if you're choosing to show or hide columns, think about recent purchases as a single thing rather than forcing the user to choose uh first recent purchase, second recent purchase, third recent purchase. That's pretty gross, and then you risk getting them out of order in a way that might mislead the consumer of the data. So that's a common application is you've got a single column that for some reason represents something that could hold one or many items, and you just need to render the same sort of visual or something in there for the one or the many. Tags are a good example, categories are a good example. Just uh all those places where you have a list and it makes sense to render uh one or multiple of the list. In some cases you choose to let the tags maybe break. Hey, I'm gonna show three of them if the columns narrow and I've got the height available. Let me go ahead and wrap these things and I'll let them spill over two lines or something. And again, this puts you back into the world of if that single cell has more data than you can display, or there's some richer version of the display that you'd like to show, you can have a progressive disclosure either on the cover or click that reveals more about the data that's inside that cell. Where that kind of thing starts to become very common and powerful is when you've got relationships in your data. So maybe a employee works at a branch, you may report the branch name, that's fine. But then uh if you've got a couple hundred employees and 30 branches and you report the branch, but you'd like to make it easy for the consumer the data to see information about the branch. And so on hover, you pop out a little card. You know that the data inside there is a branch. You're choosing to maybe show a small image and the branch name. And then on hover, maybe you reveal the hours, the address, the current number of customers inside the store. That kind of thing. And you hover on it, and then it reveals the time history. What did the last seven, fourteen, 30 days look like in terms of that employee working? So all of a sudden you're expanding a single cell into a list of cells of the same thing. And you might choose when you've got that kind of information on hand. Well, I always want to see this. I don't want to just see that they're working right now. And so maybe you find some small chart sparkling representation of where they're working over the last 10, 14, 30 days. And then you just render that. Maybe they still hover. And if one chart represents 30 days, maybe they hover and they can see 12 30 days. And you're looking at a year's worth of something. Again, it's the same idea. You take uh one of something, and even if that something represents multiple items, you can then go render many of that something. And it might be that you get many of many. And so now you've got a nice big, rich, dense grid of information. All right, what else uh complicated things that people choose to put in tables? Um, once you venture into the I'm gonna render instances of objects inside of the table, you really kind of sky's limit, especially if you've got a stylized table that's highly designed. And especially especially if you've already decided that your rotors can be two, three, four hundred pixels each. Um that's a ton of surface area to render a ton of stuff, and if your cells represent instances, you could uh you can fit a whole card in there. That's now basically your table design problem that's turned into a card design problem. I'm just gonna fill my table with cards. Definitely your only kind of doing interactive data exploration. Um but there might still be a really good reason for doing that. Like there might just be a high structure to your data where seeing snapshots of instances with rich detail, like really is still an interactive exploration. But the pieces of the data that are of most interest are numerous in some odd way, or are best visualized with some something more complicated than a single number, single string. Alright, what else people put in rows? Oh there's probably enough stuff there.